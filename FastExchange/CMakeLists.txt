cmake_minimum_required(VERSION 3.5)
project(fast_exchange)
find_package(Boost)
if(Boost_FOUND)
    set(SOURCE ${SOURCE} orderbook/vector_orderbook.cpp orderbook/matching_engine.cpp orderbook/order.cpp)
    add_library(${PROJECT_NAME} ${SOURCE})
    target_include_directories(${PROJECT_NAME} PUBLIC orderbook common message messaging users ${Boost_INCLUDE_DIRS})
endif()