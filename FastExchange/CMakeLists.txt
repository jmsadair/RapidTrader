cmake_minimum_required(VERSION 3.5)
project(fast_exchange)
find_package(Boost)
if(Boost_FOUND)
    add_library(${PROJECT_NAME} INTERFACE exchange_api.h exchange.h event_handler.h)
    target_include_directories(${PROJECT_NAME} INTERFACE .)
    target_include_directories(${PROJECT_NAME} INTERFACE ${Boost_INCLUDE_DIRS})
    target_include_directories(${PROJECT_NAME} INTERFACE common message messaging orderbook users)
endif()